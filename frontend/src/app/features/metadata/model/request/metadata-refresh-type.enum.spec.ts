import {describe, expect, it} from 'vitest';

// TODO(frontend-coverage): Replace this stub with real coverage for frontend/src/app/features/metadata/model/request/metadata-refresh-type.enum.ts.
describe.skip("metadata-refresh-type.enum TODO stub", () => {
  it('TODO: add real coverage', () => {
    expect(true).toBe(true);
  });
});
